export interface Certification {
    id: string;
    title: string;
    issuer: string;
    date: string;
    color: string;
    credentialId: string;
    description: string;
    imageUrl?: string;
}
